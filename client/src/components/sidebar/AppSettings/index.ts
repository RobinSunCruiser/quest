/**
 * @fileoverview Barrel exports for application settings components and types.
 * Provides centralized access to the main AppSettings container and individual panel components.
 * @module Components.Sidebar.AppSettings
 */

/** Main container component that orchestrates all settings using the global app settings store */
export { AppSettingsWindow as AppSettings } from './AppSettingsWindow';
