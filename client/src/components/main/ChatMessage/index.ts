/**
 * @fileoverview Entry point for ChatMessage components. Provides a clean import interface
 * for chat message rendering functionality.
 * @module Components.Main.ChatMessage
 */

export { ChatMessage } from './ChatMessage';
